<UserControl x:Class="Power_Hand.Features.FeatureApp.FeatureEmployee.AddEditEmploeePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Power_Hand.Features.FeatureApp.FeatureEmployee"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:AddEditEmployeePageVM}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <local:AddEditEmploeeForm Grid.Column="0" DataContext="{Binding EmployeeFormVM}" />

        <local:SearchEmploeesListView Grid.Column="1" DataContext="{Binding SearchEmployeesVM}"/>

    </Grid>
</UserControl>
