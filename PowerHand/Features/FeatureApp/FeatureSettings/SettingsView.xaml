<UserControl x:Class="Power_Hand.Features.FeatureApp.FeatureSettings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Power_Hand.Features.FeatureApp.FeatureSettings" 
             d:DataContext="{d:DesignInstance Type=local:SettingsVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="5" Orientation="Vertical" Background="White" MinWidth="200">
            
            <TextBlock HorizontalAlignment="Center" Text="{Binding LastUpdate}" />

            <TextBox Padding="5" Text="{Binding MyLastUpdate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

            <Button Content="Update Database" Command="{Binding ChangeLastUpdateCommand}" />

        </StackPanel>
        
    </Grid>
</UserControl>
