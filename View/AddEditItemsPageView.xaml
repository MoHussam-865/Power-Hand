<UserControl x:Class="Power_Hand.View.AddEditItemsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Power_Hand.View"
             xmlns:viewmodels="clr-namespace:Power_Hand.ViewModels"
            d:DataContext="{d:DesignInstance Type=viewmodels:AddEditItemPageVM}"
            xmlns:components="clr-namespace:Power_Hand.Utils.Component"
            xmlns:Views="clr-namespace:Power_Hand.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Views:ItemFormView Grid.Column="0" DataContext="{Binding ItemFormVM}"/>

            <Views:ItemsGridView Grid.Column="1" DataContext="{Binding GridItemsVM}" />

        </Grid>
    
</UserControl>
